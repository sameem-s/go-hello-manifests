apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-hello-service.fullname" . }}
  labels:
    {{- include "go-hello-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-hello-service.selectorLabels" . | nindent 4 }}
